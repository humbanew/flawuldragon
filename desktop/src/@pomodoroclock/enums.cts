export enum EPCPomodoroStatus {
	None = 'none',
	Running = 'running',
	Paused = 'paused',
	Done = 'done'
}

export enum EPCPomodoroType {
	Work = 'work',
	LongBreak = 'long break',
	Break = 'break',
}
